{
  "buildCommand": "cd client && yarn install && ./node_modules/vite/bin/vite.js vite build",
  "outputDirectory": "client/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/server/(.*)",
      "destination": "/server/index.js"
    }
  ]
}
